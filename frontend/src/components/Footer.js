import {Link} from "react-router-dom";

export default function Footer(){
    return(
        <footer>
            <p>footer</p>
            {/**
             * links
             * copyright
             */}
        </footer>
    );
}